import { PopoverArrowDirective } from "./popover-arrow.directive";

describe("PopoverArrowDirective", () => {
   it("should create an instance", () => {
      const directive = new PopoverArrowDirective(null, null);
      expect(directive).toBeTruthy();
   });
});
